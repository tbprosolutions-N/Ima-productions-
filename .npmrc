# Use legacy-peer-deps to avoid peer dependency conflicts (e.g. React 18)
# If you see "Unknown env config legacy-peer-dep": remove NPM_CONFIG_LEGACY_PEER_DEP (typo) from env;
# use NPM_CONFIG_LEGACY_PEER_DEPS=true (with S) if setting via env.
legacy-peer-deps=true
